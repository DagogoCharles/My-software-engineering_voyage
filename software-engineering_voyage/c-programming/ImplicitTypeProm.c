#include <stdio.h>

int main()
{
	double x = 50.0f;

	printf("%f", x); //printf takes a double and promotes the val of x.
       	//floats and double use the same parameters, a float is smaller than a double so it can fit inside//

	return 0;
}	
