/**
 * Hey there!
 * this is a unary operator
 *To add 1 to a var x; ==> x++;
 adding more than 1 eg. 5 to x ==> x = x + 5;
 Note that; ++ has to be put before the output var ==> sum = ++x;
 Same applies for decrement.
 */

#include <stdio.h>

int main()
{
	return 0;
}	


